apiVersion: v1
kind: Service
metadata:
  name: localagent-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: localagent-api
  ports:
    - name: http
      port: 8000
      targetPort: http
