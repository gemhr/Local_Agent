apiVersion: v1
kind: Secret
metadata:
  name: localagent-secrets
type: Opaque
stringData:
  LOCAL_AGENT_DATABASE_URL: "postgresql+asyncpg://localagent:replace-me@postgres:5432/localagent"
  LOCAL_AGENT_REMOTE_API_KEY: "replace-me"
  LOCAL_AGENT_JWT_PUBLIC_KEY: "replace-me"
