# Runtime Capability Matrix

## Stage 3 WP3 Security Baseline Capabilities

| Capability | Status | Coordinated | Owner / path | State | Failure behavior | Recovery | Legacy | Evidence | Limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| File Tool application read-root authorization | SUPPORTED | 是 | ResourceAuthorizationService + frozen extractor catalog / multiple canonical roots | 无独立持久化；Settings startup snapshot | outside/invalid/type mismatch 在 ToolExecutionService 前固定 safe denial；Tool events与final model均为0 | 不重试/不 fallback | 同一 AgentRouter Gate | `tests/test_stage3_wp3_security_baseline.py` + `tests/test_stage3_wp3_security_e2e.py` | 仅 `list_files` / `analyze_excel` READ；UNC不支持；不是OS Sandbox；存在TOCTOU limitation |
| Wiki configured-output containment | SUPPORTED | 不适用 | WikiCrawler component invariant | 本地业务文件 | 非法remote leaf或越界candidate skip item并记录固定低基数code | 不重试被拒绝item | 不适用 | WP3 Wiki deterministic fake-HTTP regression | Wiki endpoint配置化、全仓日志清洗不在本WP |
| PRODUCTION local API loopback boundary | SUPPORTED | 是 | Settings startup validation | 无 | non-loopback、hostname或非法base URL fail closed | 重启修正 | 同配置 | WP3 Settings security tests | 无authenticated human IAM、无inbound TLS；仅认证同机loopback baseline |
| HTTP raw-body payload boundary | SUPPORTED | 是 | `RequestBodyLimitMiddleware` + frozen `RequestPayloadPolicy` | 无 | invalid/duplicate Content-Length 固定400；声明或实际 body 超限固定413；endpoint/Run不启动 | 不重试/不 fallback | application-wide，同一入口 | `tests/test_stage3_wp3b_payload_e2e.py` | 完整缓冲最多1 MiB；不是Rate Limit、DLP或Runtime Budget |
| HTTP semantic field boundary | SUPPORTED | 是 | FastAPI/Pydantic schema + frozen `RequestPayloadPolicy` | 校验失败无业务 mutation | chars/count/range 越界固定422并阻止service调用 | 修正请求后新请求 | 同一endpoint schema | `tests/test_stage3_wp3b_payload_policy.py` + payload E2E | 默认422 detail可能回显输入；WP3-C候选 |
| WAF / generic abuse protection | NOT_IMPLEMENTED | 否 | 无（未建立production Owner） | 无 | fixed payload bounds只限制单请求资源，不执行generic abuse/bot检测、distributed filtering、per-principal traffic policy或WAF rules | 无 | 否 | formal negative capability audit | Payload Gate != WAF；不得声称完整DoS/abuse protection |
| Prompt Injection protection | PARTIALLY_SUPPORTED | 是 | `ContextBuilder` role binding + typed `AgentAdapterResult` / `StepResult` denial propagation + Synthesis `DENIAL_DOMINATES` | typed denial仅在当前Run内部经StepResultStore传播；无dedicated Event/Journal/Snapshot fact | code-owned controls才可进入system；Tool Result=`TOOL_RESULT/UNTRUSTED_EXTERNAL/user`；Step Result=`STEP_RESULT/USER_CONTENT/user`；任一typed denial在prompt/model前直接safe denial | Recovery不能重建runtime-internal typed denial | explicit Legacy保持同一Tool/Step data role与denial-before-synthesis语义 | `tests/test_stage3_wp3c_context_trust_boundary.py` + `tests/test_stage3_wp3c_denial_integrity.py` + `tests/test_stage3_wp3c_injection_e2e.py` | 模型仍可受恶意自然语言影响或复述System Prompt；无generic classifier/WAF/DLP/Human IAM/full Sandbox/HITL；mixed denial丢弃成功partial result；Command Injection与SSRF为NOT_APPLICABLE_CURRENT_INVENTORY |
| SQL Injection protection | SUPPORTED | 是 | code-owned SQL structure owner + DB-API parameter binding + test-only WP3-D AST owner/sink Gate | 复用 Memory/Journal/Snapshot/Checkpoint 既有 SQLite persistence；无新增 schema 或安全状态 | 动态 statement、未知 receiver、owner expansion、`executescript` 与 exception drift 在静态 Gate fail closed；HTTP validation/DB failure保持既有无业务越权与用户可见非泄漏 | 不改变 validation-only Recovery；不从正文重建 SQL | explicit Legacy复用同一 Memory/SQLite owner contracts | `tests/test_stage3_wp3d_sql_injection_guard.py` + `tests/test_stage3_wp3d_sql_injection_runtime.py` + `tests/test_stage3_wp3d_sql_injection_e2e.py` | 仅限 current LocalAgent production SQLite inventory；No generic SQL firewall；No NL2SQL；No SQL Tool；FTS query-language semantics与LIKE wildcard semantics保留；schema-metadata PRAGMA仅精确只读启动例外；Chroma不是direct SQL owner；新增owner/数据库技术必须re-gate |

以上是 Phase 3 实现/测试事实，不是 WP3 Final Gate PASS。Prompt Injection仅为 `PARTIALLY_SUPPORTED`；SQL Injection protection 的 `SUPPORTED` 仅适用于 current LocalAgent production SQLite inventory。二者均不代表Sandbox、IAM、TLS、Rate Limit、DLP、WAF、egress isolation或通用injection prevention已实现。

状态词仅允许：`SUPPORTED`、`PARTIALLY_SUPPORTED`、`CONTRACT_ONLY`、`NOT_IMPLEMENTED`、`LEGACY_ONLY`、`DEPRECATED`。存在类型、枚举或测试 seam 不等于生产能力已支持。

| capability | status | default_path | owner | persistence | failure_behavior | recovery_behavior | legacy_support | tests | known_limitations |
|---|---|---|---|---|---|---|---|---|---|
| Coordinated Runtime | SUPPORTED | 是 | ChatRuntimeSelector + CoordinatedRuntimeFactory + RunCoordinator | Journal；Snapshot opt-in | 所选路径内安全失败并发 terminal | 仅可做 validation | 否 | runtime full e2e/default entry | 当前 `/api/chat` 为单 Agent；typed multi-step 真实默认并发上限为 2 |
| Legacy rollback | SUPPORTED | 否，显式 `LEGACY` | ChatService Legacy AgentLoop | 既有 Memory；无完整 Runtime Journal/Snapshot | Legacy 内失败并安全映射 | 无 Runtime recovery | 是 | runtime legacy boundary/mode e2e | 仅兼容回滚路径，不宣称完整 Coordinated 能力 |
| Parallel execution | SUPPORTED | 默认单 Agent 路径不并行 | ParallelExecutor + Scheduler | Runtime events/state | 单 step 失败按现有 policy 收口 | 无自动恢复 | 部分/非统一 | parallel execution/scheduler | 默认 factory policy `max_concurrency=2`（真实 effective 全局并发上限）；`ParallelExecutor` 构造默认 1 被 policy 覆盖，命名清理属 WP2 |
| Budget | SUPPORTED | 是 | BudgetLedger | Snapshot budget projection；event counts | 超限 fail closed | Snapshot 仅供 validation | Legacy 有基础 Run budget | budget/runtime e2e | 进程内 ledger，不是跨进程配额 |
| Retry | SUPPORTED | 是 | RetryExecutor + policy | attempt index/evidence，不持久化 policy state | 按类型、幂等与预算判断 | 不自动重放历史 attempt | 既有调用链部分支持 | retry model/tool tests | 不提供跨进程 retry continuation |
| Fallback | SUPPORTED | Model routing 内 | ModelInvocationRouter + ModelRoutingPolicy | Model event 安全事实 | candidates 耗尽后类型化失败 | 不切换 Runtime | Model 层可用 | model routing/invocation | 仅 Model candidate fallback，不是 Runtime fallback |
| Circuit breaker | SUPPORTED | 是 | ModelCircuitBreakerRegistry | 进程内 state/snapshot | open 时 fail fast/route | timeout 后 half-open | 共享 model layer | model circuit breaker | 非分布式 circuit state |
| Tool idempotency | SUPPORTED | Tool contract | ToolExecutionService + invocation contract | key 仅 digest evidence | 不安全结果禁止 retry | Recovery 只评估 evidence | 通过 adapter 部分支持 | tool contract/recovery | 不构成全系统 exactly-once |
| Tool side-effect evidence | SUPPORTED | Tool completion | AttemptSideEffectTracker + ToolCompletedPayload | Journal safe evidence | Unknown/committed 均保守处理 | 可给出 reconciliation decision | 部分 | tool evidence/recovery | 不保存原始参数/输出；不自动补偿 |
| Resource lease | SUPPORTED | Tool attempt | ToolConcurrencyController + ToolResourceLease | 进程内 worker snapshot | timeout/detached 延后释放 | Shutdown 统计/等待，不恢复 lease | 部分 | tool concurrency/resources | 不是跨进程 lease |
| Tool Registry | SUPPORTED | 是 | `core/runtime/tool_registry.py` ToolRegistry（APPLICATION_SCOPE，startup 冻结） | 进程内只读 registry | freeze 后 register → `TOOL_REGISTRY_FROZEN`；read-before-freeze → `TOOL_REGISTRY_NOT_FROZEN` | 不涉及 | 否 | tool registry / tool execution integration / startup configuration | INTERNAL_RC；仅内部枚举/解析；无 `/api/tools`、无跨进程、无动态注册 |
| Tool Descriptor | SUPPORTED | 是 | immutable ToolDescriptor（name + description） | 进程内只读 | name 非安全 / description 非法 → 注册失败（`TOOL_REGISTRY_INVALID`） | 不涉及 | 否 | tool registry tests | INTERNAL_RC；不含 input/output schema、version、capability、risk/permission/approval（WP2-B） |
| Static startup registration | SUPPORTED | 是 | `server.py::lifespan()` → `register_all_tools(registry)` → freeze → 注入 AgentRouter | 进程内只读 | 非法/重复/binding mismatch → startup fail，never READY | 不涉及 | 否 | startup configuration / tool registry | 无运行期 add/remove/replace/hot reload |
| Duplicate Tool identity | SUPPORTED | 是 | ToolRegistry.register | 进程内只读 | duplicate → `TOOL_REGISTRY_DUPLICATE`，保留 original binding（无 last-write-wins） | 不涉及 | 否 | tool registry tests | 覆盖「重复注册静默覆盖」bad case |
| Internal Tool discovery | SUPPORTED | 是 | ToolRegistry resolve/require/registrations/descriptors/contains | 进程内只读 | require unknown → `TOOL_NOT_REGISTERED` fail closed；resolve unknown → None（untrusted） | 不涉及 | 否 | tool registry / execution integration | 仅内部确定性枚举；无 search/filter/tag API |
| All-Tool adapter execution | SUPPORTED | 是 | ToolAdapter → ToolExecutionService（4/4 production Tools） | ToolCompletedPayload safe evidence | adapter 失败安全收口，无 legacy direct-call fallback | Recovery 只读评估 | 旧 direct-call 路径已移除 | tool execution integration / migration tests | `list_files`/`analyze_excel` 已迁移；ToolExecutionService 不做 name resolution |
| Static frozen Tool policy | SUPPORTED | 是 | `core/runtime/tool_governance.py` ToolPolicyCatalog（APPLICATION_SCOPE，startup register/validate/freeze） | 进程内只读；不持久化 | 任一 registered Tool 缺 policy / 引用未知 Tool/Agent / allowed set 为空 / risk fact 或 approval rule 非法 → freeze fail，startup never READY | 不涉及 | 否 | tool governance tests / startup configuration | INTERNAL_RC；无 hot reload / remote policy / runtime mutation |
| Agent Tool permission | SUPPORTED | 是 | ToolGovernanceService.authorize_tool（静态 Permission，explicit allowed Agent IDs） | 不持久化；仅 outcome/code | 未知 principal → `TOOL_GOVERNANCE_UNKNOWN_PRINCIPAL`；explicit deny → `TOOL_PERMISSION_DENIED`；missing policy → `TOOL_GOVERNANCE_POLICY_MISSING`，均 fail closed 且不调用 build/execute | 不涉及 | 否 | tool governance tests | 4 Tool × 5 Agent = 20 条显式授权；Agent capability ≠ Tool permission；非 filesystem/path 授权 |
| Tool risk classification | SUPPORTED | 是 | ToolGovernanceService.evaluate_invocation（exact full-combination allowlist：`frozenset(static facts)` + `side_effect_kind` + `idempotency` → effective risk） | 不持久化；仅 risk_level/risk_facts | 未冻结完整组合（含 static/dynamic 各自已知但组合未批准、multiple static facts、未知 dynamic enum）→ `TOOL_RISK_UNCLASSIFIED` fail closed | 不涉及 | 否 | tool governance tests | 5 个冻结完整组合：`{ARBITRARY_LOCAL_FILESYSTEM_READ}+NONE+READ_ONLY→MEDIUM`、`{SYSTEM_INFORMATION_READ}+NONE+READ_ONLY→LOW`、`{}+NONE+READ_ONLY→LOW`、`{}+LOCAL_STATE_MUTATION+IDEMPOTENT_WITH_KEY→MEDIUM`、`{}+LOCAL_STATE_MUTATION+NON_IDEMPOTENT→HIGH`；不实现通用 risk algebra（不取 max）；side effect ≠ risk taxonomy |
| Approval requirement gate | SUPPORTED | 是 | effective risk >= policy threshold（HIGH）→ `APPROVAL_REQUIRED` → terminal pre-execution safe denial | 不持久化；仅 code | 无 approval evidence 时不执行、不发 TOOL_STARTED/COMPLETED | 不涉及 | 否 | tool governance tests | complex `NON_IDEMPOTENT_SIMULATION` → APPROVAL_REQUIRED；无 human approval UI / durable pause-resume / approval endpoint |
| All-current Tool policy coverage | SUPPORTED | 是 | `register_default_tool_policies(catalog)` 恰好注册 4 条 policy，Tool 集合与 frozen ToolRegistry 一致 | 进程内只读 | missing/duplicate policy → startup fail | 不涉及 | 否 | tool governance tests / startup configuration | 当前 4 Tool 均有 policy；Drift 由 coverage 测试与 Catalog freeze 双向阻止 |
| Default COORDINATED Tool full integration E2E | SUPPORTED | 是 | `server.app` + production lifespan + ChatService / dynamic planning / AgentRouter / Registry / Governance / ToolExecutionService / OutputGate | 临时隔离的 Memory、Journal、Observability checkpoint；Snapshot 关闭 | success 正常交付；`APPROVAL_REQUIRED` 在 Tool execution 前固定 safe denial，Run 仍正常交付 | 不自动恢复 | 否 | `tests/test_stage3_wp2_tool_e2e.py` success E2E + governance non-ALLOW E2E | offline deterministic model seam 仅替代外部模型输出；production chain 原已实现；不代表 human approval、path authorization、sandbox 或 dedicated governance event |
| Retrieval stage runtime | SUPPORTED | 是 | RetrievalExecutionService | 安全 stage events/counts | 类型化失败、预算/取消传播 | 只读 validation 不 replay | Legacy 通过既有 router 部分支持 | retrieval integration/execution | 不持久化 chunk/query 正文 |
| Event streaming | SUPPORTED | 是 | RuntimeEventChannel + ChatStreamCompatibilityAdapter | Journal-first 后 transport | backpressure、abort、安全 transport error | 不重放 stream | Legacy 有旧文本 stream | event channel/stream adapter | 自定义 `text/plain` 协议，不是 SSE/WebSocket |
| Journal-first | SUPPORTED | Coordinated | RuntimeEventChannel + RunEventJournal | InMemory/SQLite append-only | append 失败阻止 transport；部分发布有 evidence | Recovery 读取 tail | Legacy 未完整接入 | event journal/partial publication | 不保证跨进程 exactly-once consumer |
| Snapshot | PARTIALLY_SUPPORTED | 默认关闭，显式 opt-in | CheckpointCoordinator + SnapshotStore | SQLite/InMemory Snapshot v1 | 严格 schema/digest，失败不伪造 | 提供 validation 输入 | 否 | snapshot/checkpoint | 未自动用于继续执行；默认 `LOCAL_AGENT_SNAPSHOT_ENABLED=false` |
| Recovery validation | PARTIALLY_SUPPORTED | Snapshot 启用时可用 | RecoveryValidator | 读取 Snapshot v1 + Event v1/v2 | 不兼容/损坏/不足证据 fail closed | 只返回 Assessment | 否 | recovery integration/version compatibility | 不等于 automatic recovery |
| Recovery execution | NOT_IMPLEMENTED | 否 | 无 | 无 | 不启动任何 adapter | 不适用 | 否 | negative recovery contract tests | Validator 明确无 AgentState writer |
| Replay | NOT_IMPLEMENTED | 否 | 无 | 无 | 所有 replay flags 为 false | 不适用 | 否 | recovery contract/capability matrix | 无 model/tool/retrieval replay |
| Observability | SUPPORTED | 是 | RuntimeObservabilityDispatcher + projectors | consumer checkpoint + recorder | 失败隔离，不改变 Runtime/Journal | 无业务恢复 | shared application resource | observability integration/fault | in-process queue；指标标签受 allowlist 限制 |
| Semantic Memory Formation | SUPPORTED | `COORDINATED` + `persist=True` + canonical `direct` scope | StepResultCommitter hook + SemanticMemoryFormation + AdvancedMemoryStore | conversation exchange 先独立提交；每个 accepted candidate 独立 transaction；`REMEMBER` 必须显式提供 `predicate_resolution`（REGISTERED / OPEN） | model/parser/persistence/observation failure typed 收口，不改变 delivered output、final Step 或 Run terminal；missing/unknown resolution、invented/alias predicate ID、category/value mismatch 全部 fail closed | 无 crash replay；仅同一 execution prepared-record retry 幂等 | 否 | `tests/test_semantic_memory_formation.py`、`tests/test_semantic_memory_formation_hook.py`、`tests/test_predicate_registry.py` | 无 cross-Run dedup/conflict/NO_CHANGE/supersede/retrieval/context injection；语义误分类与 hard-crash gap 为已知限制 |
| Canonical Predicate Registry（v1） | SUPPORTED | `CanonicalPredicateRegistry` code-owned 只读 | v1 只冻结 `project.database`、`project.package_manager`、`engineering.public_network_allowed`；`logical_key` 只能由 LocalAgent 从 accepted registry entry 编译 | 无（纯代码元数据） | dynamic registration、config plugin、DB registry table、ontology、alias table、external API、`NEW_PREDICATE` | INTERNAL_RC | `tests/test_predicate_registry.py` | `user.response_style` / `project.runtime` / `engineering.network_access` 不在 v1；无 alias/underscore normalizer；`project_database`/`project.db`/`database_backend` 均 fail closed |
| Predicate Resolution Contract | SUPPORTED | Formation proposal：`REMEMBER` 必填 `predicate_resolution`；`REGISTERED` 需 exact registry ID，`OPEN` 需 null ID | LocalAgent exact registry lookup + category/value 校验后编译 canonical `logical_key`；`OPEN` → `logical_key=None` → INSERT-only | 无 | invalid REGISTERED 静默降级 OPEN；Model 输出 `logical_key`/invented ID；OPEN + non-null ID；unknown resolution | PUBLIC_VERSIONED schema v1 | `tests/test_semantic_memory_formation.py` | `SEMANTIC_PREDICATE_CLASSIFICATION=PROBABILISTIC`；classify 为 OPEN 的注册事实允许 unkeyed INSERT，不会 mutate keyed partition（Evaluation target） |
| Memory Lifecycle Resolution（INSERT / NO_CHANGE / SUPERSEDE） | SUPPORTED | keyed `SEMANTIC` + `direct` scope（`logical_key` 非空） | `MemoryLifecycleResolver`（decision Owner）+ `AdvancedMemoryStore.resolve_semantic` | 决策与 mutation 在同一 `BEGIN IMMEDIATE` transaction；`(agent_id, memory_scope, SEMANTIC, logical_key)` partition；typed strict equality | 任一 insert/update/relation failure 全事务 rollback；失败 typed 收口 | lazy repair：只修复被新请求触发的 partition；历史 partition 可保留 WP2 duplicate | 否 | `tests/test_memory_lifecycle.py`、`tests/test_memory_lifecycle_forget.py` | no-key 一律 INSERT；typed equality 不做 LLM/Embedding 语义等价；`Postgres` vs `PostgreSQL` 视为不同（KNOWN_LIMITATION） |
| Explicit Forget（all-version redaction） | SUPPORTED | original-user deterministic forget cue + forget model proposal + exact registry-backed existing-key membership | `ExplicitForgetIntentParser` + `AdvancedMemoryStore.forget_semantic_partition` | 单事务 all-version status/redaction/relation cleanup；tombstone=`[FORGOTTEN]`/`{}`/relation None/key retained；new canonical path 只允许 registry-backed keys | 任一 row redaction failure 全事务 rollback；repeat forget 幂等 `ALREADY_FORGOTTEN` | forget 与 remember 对同一 exchange 互斥；logical key 保留供 exact targeting 与幂等；OPEN/unkeyed 不提供 semantic user-chat forget（NOT_IMPLEMENTED） | 否 | `tests/test_memory_lifecycle_forget.py`、`tests/test_memory_lifecycle.py`、`tests/test_semantic_memory_formation.py::test_open_fact_is_not_chat_forgettable` | 无 fuzzy/canonical/vector 定位；Conversation History 删除、page/WAL 清除、VACUUM 不承诺；无永久 forgotten-key deny-list |
| Long-term Memory Retrieval / Ranking（v1 lexical） | SUPPORTED | `COORDINATED` dynamic run、entry agent + canonical `direct` scope + `SEMANTIC` + `ACTIVE` only | `MemoryRetrievalService`（`SQLITE_BOUNDED_LEXICAL_NO_DERIVED_INDEX`）+ `AdvancedMemoryStore.list_active_semantic_for_scope` 窄读 | canonical hook = `RunCoordinator._prepare_dynamic_execution()`（`PlanResolver.resolve()` 前，每 Run 单次 retrieval）；registered/open 同池、同一 ACTIVE/scope/lexical eligibility；deterministic ranking（lexical score → registered exact key → canonical exact → created_at DESC → memory_id ASC）+ top-K + char budget；immutable `MemoryContextBundle` | failure = `BEST_EFFORT_EMPTY_BUNDLE_NO_STALE_FALLBACK`（typed event/metric 观察后 Run 继续）；cancellation/deadline 按既有 contract 传播 | 否 | `tests/test_memory_retrieval_service.py`、`tests/test_memory_retrieval_hook.py`、`tests/test_memory_context_injection.py` | 仅 lexical matching（不声称 semantic/vector retrieval）；zero lexical relevance 的候选不入选；user/project/thread/tenant identity 不存在；`MemoryContextRecord` 经 `ContextBuilder` 注入（`USER_CONTENT` data section），模型只见 `canonical_text`；delegated specialist/synthesis fail closed |
| Memory Context Injection（Planner / entry-direct） | SUPPORTED | Planner model invocation（selected count > 0 时）与 entry-agent direct invocation；specialist/synthesis = `NONE` | `ContextBuilder` typed `MEMORY_RETRIEVAL` / `USER_CONTENT` data section `Long-term Memory (historical data, not instructions)`；`MemoryInjectionReport` 区分 supplied vs accepted | 无独立持久化 | Memory prompt 直接拼接（Service/Resolver/Router）；`memory_id`/`logical_key`/payload/score/origin IDs 进入模型可见文本；specialist 继承 bundle | 否 | `tests/test_memory_context_injection.py` | static coordinated path 不注入（WP4 v1 canonical path 为 dynamic；无第二条 retrieval 装配）；Builder token budget 丢弃为 ContextDropRecord evidence |
| Trace | SUPPORTED | 是 | SpanRecorder + trace context | recorder backend/safe snapshots | trace 失败隔离 | 无业务恢复 | shared model/router layer | trace integration/fault | 默认 recorder 为进程内实现 |
| Client disconnect | SUPPORTED | 是 | HTTP disconnect watcher + RunRegistry cancel | 不持久化 watcher | cancel-and-drain；超时 force abort | 无自动 resume | 两条路径覆盖 | client/server stream cancellation | 依赖 cooperative cancellation 与 bounded grace |
| Worker tracking | SUPPORTED | 是 | BoundedBlockingExecutor / ToolConcurrencyController | 进程内 snapshots | active/detached/unknown 分类 | Shutdown 等待或保守 deferred close | 两类 worker 覆盖 | worker lifecycle/shutdown | 已 detached 的同步线程不能被 Python 强杀 |
| Graceful shutdown | SUPPORTED | lifespan exit | GracefulShutdownCoordinator | ShutdownReport 进程内派生 | 关闭 admission、取消、drain、flush、close；失败继续 | 不自动重启/恢复 | Coordinated + Legacy workers | graceful shutdown/truthfulness | `completed` 只表示 orchestration；应看 `fully_closed` |
| Deterministic Fault Injection | SUPPORTED | 无；仅 `TEST_SCOPE` / 显式 operation scope | FaultInjectionScope/Controller | test recorder/report；不进生产存储 | 确定性 typed fault；生产路径 controller 为 `None` | 不拥有 recovery | 生产未启用 | day24 fault matrix + production isolation | 个别 FaultPoint 仍由 support report 区分 SUPPORTED/CONTRACT_ONLY/NOT_APPLICABLE |
| Production Fault Enablement | NOT_IMPLEMENTED | 否 | 无 | 无 | 无 Settings/API/prompt/tool 激活入口 | 无 | 否 | production isolation + capability negative assertion | `production_enablement=false` |
| Random / Probabilistic Chaos | NOT_IMPLEMENTED | 否 | 无 | 无 | 无随机或概率触发 | 无 | 否 | capability negative assertion | FaultTrigger 只有确定性触发 |
| Cross-process Registry | NOT_IMPLEMENTED | 否 | 无 | 无 | 进程退出即丢失 | 无 | 否 | capability negative assertion | RunRegistry 是进程内对象 |
| Exactly-once | NOT_IMPLEMENTED | 否 | 无系统级 owner | 局部 idempotency/journal evidence | 仅局部去重与保守失败 | 无跨进程 reconcile executor | 否 | idempotency/event consumer tests | 局部 at-most-once/幂等证据不能提升为全系统 guarantee |
| Automatic compensation | NOT_IMPLEMENTED | 否 | 无自动编排 owner | 仅 compensation evidence | 不自动触发补偿 | 只报告/评估 | 否 | tool contract/fault invariant | adapter 可报告结果，但 Runtime 不自动执行策略 |
| Step result rehydration | NOT_IMPLEMENTED | 否 | 无 | Snapshot 只保存 TextSummary/digest | 缺正文时保持 unavailable | `output_reconstruction_supported=false` | 否 | snapshot/recovery capability tests | 不从当前 Registry/Memory 回填历史正文 |
| Multi-process server | NOT_IMPLEMENTED | 否 | 无 | 无 | 不支持 multi-worker/多进程部署 | 无 | 否 | deployment contract tests | Win 部署合同强制 exactly one server application process；禁止 `--workers N`、gunicorn、multi-process Runtime |
| Windows Service wrapper | NOT_IMPLEMENTED | 否 | 无 | 无 | 不提供 service wrapper/NSSM/WinSW/Task Scheduler 集成代码 | 无 | 否 | deployment contract tests | 正式部署合同是 single foreground process，由 operator/企业内部环境托管 |
| Docker / Compose | NOT_IMPLEMENTED | 否 | 无 | Windows filesystem persistence | 不支持 Docker/Compose 部署 | 无 | 否 | deployment contract tests | Windows Native = Stage 3 唯一 certified target；Docker/Compose 不属于当前完成标准 |
| Health / Readiness endpoint | SUPPORTED | 是 | `core/runtime/health.py` 纯诊断投影（Diagnostic Projection Owner，非 Authority）；FastAPI endpoint 仅 Reader | 无（不持久化；StartupDependencySnapshot 是进程内 immutable 启动快照） | `GET /health` 证明未进入 terminal CLOSED / fatal unavailable；`GET /readyz` 证明 services + lifecycle READY + admission ACCEPTING（唯一 allowlisted KB degradation 仍 ready） | 不触发 recovery/retry；不写回 lifecycle/admission/dependency | 否 | `tests/test_health_readiness.py`（pure matrix + ASGI + alias 404 + schema 安全）+ `tests/test_startup_configuration.py`（KB snapshot 接线） | startup-only；无 continuous monitoring；无 post-start dependency aggregate health；STARTING/CLOSED/draining 完整窗口不保证生产 HTTP 可观察 |
| Startup readiness handshake | SUPPORTED | 是 | `core/client_readiness.py`（ReadinessWorker QThread，一次 startup probe）；Owner = MainController | 无 | Client 构造 UI 后启动 background QThread 做 bounded `GET /readyz` probe（1s / 30s / 0.5s，jitter=none）；ready → 首屏 history fetch；unavailable → 一次固定 safe 系统消息 | 不阻塞 Qt UI event loop；deadline/cancel 后 worker 退出 | 否 | `tests/test_client_readiness.py`（probe matrix + deadline bounded + interruption + trust_env + UI non-blocking + history gating） | startup-only；无 continuous monitoring / auto reconnect / manual button；version compatibility = NOT_IMPLEMENTED / DEFER_TO_WP4 |
| Client HTTP Proxy Governance | SUPPORTED | 是 | Settings + main.py client sessions（聊天/历史/搜索/取消）+ ui/chat_panel.py plumbing + ui/memory_dialog.py memory session | 无 | 显式 `session.trust_env = settings.client_trust_env`（main.py）与 constructor 透传 `client_trust_env`（ui/）；非法显式值 fail closed | 无 | 否 | deployment contract/client proxy tests（4 Session inventory + 行为证据） | `LOCAL_AGENT_CLIENT_TRUST_ENV` 控制 Desktop Client → LocalAgent Server 全部 4 个 Session；与 `LOCAL_AGENT_REMOTE_TRUST_ENV` 完全独立 |
| Persistence preflight | SUPPORTED | 是 | Persistence Migration Coordinator + Store owner（`core/persistence_migration.py`） | 无（read-only；不创建/修改 DB） | Server startup 自动 read-only preflight（`PRAGMA quick_check` + physical shape + 版本事实）；migration-required / unsupported / failed 阻止 READY | 不触发 migration/repair/restore/rebuild；endpoint 不执行 preflight | 否 | `tests/test_persistence_preflight.py`、`tests/test_startup_configuration.py`、`tests/test_persistence_migration.py` | FULL operator preflight 增加逐 record 校验；Chroma marker validation 在 VectorDB 构造后执行 |
| Explicit SQLite migration | SUPPORTED | 否（显式 `SCRIPT_ROLE`） | Persistence Migration Coordinator + Memory/Journal/Checkpoint migration function | 单 Store transaction | `scripts/manage_persistence.py migrate --backup-confirmed`；缺 flag 或 unsupported from-state 拒绝且零 mutation；每 Store 独立 transaction | 部分 commit 保留并如实报告；rerun 从实际 facts 继续 | 否 | `tests/test_persistence_migration.py` | 无 cross-store transaction；Server startup 绝不自动迁移已有数据 |
| Memory versioned migration | SUPPORTED | 显式 | `MemoryManager` Store-owned（`memory_preflight`/`memory_migrate`） | Memory SQLite `PRAGMA user_version=2` | current-unversioned → 版本 2 adoption；v1（无 `long_term_memory`）→ additive 新增 Long-term Memory 结构；唯一 allowlisted pre-additive legacy → additive columns + backfill + tables/indexes/FTS/triggers + Long-term Memory；ambiguous → `PERSISTENCE_SCHEMA_UNSUPPORTED` | 失败 rollback（version/shape 不前移） | 否 | `tests/test_persistence_migration.py`、`tests/test_persistence_preflight.py`、`tests/test_advanced_memory_persistence.py` | 普通 constructor 不再隐式建表/ALTER（只对全新 DB 初始化 v2）；不修改业务 row 正文 |
| Journal known physical migration | SUPPORTED | 显式 | `SQLiteRunEventJournal` Store-owned | Journal SQLite exact physical signature（无 DB-level version） | 唯一 allowlisted legacy = 缺 nullable `span_id`/`parent_span_id` 的表；单事务 ADD 两列 + index；绝不 rewrite 历史 row/version/digest | v1 row 继续以 v1 digest 语义读取；不升级 row version | 否 | `tests/test_event_journal.py`、`tests/test_persistence_migration.py` | 未来新增 physical delta 必须重新决定 DB-level version |
| Checkpoint explicit recreate | SUPPORTED | 显式 | `SQLiteEventConsumptionCheckpointStore` Store-owned | Checkpoint SQLite exact table shape（无版本） | 不兼容 shape → 显式 recreate（单事务 drop/recreate table）；derived-state recreation，丢弃历史消费 offset | 两个 consumer 可重新写入；不修改 Memory/Journal | 否 | `tests/test_persistence_migration.py` | checkpoint backup optional；startup 仍 required（不兼容时先显式 recreate 再 READY） |
| Chroma compatibility marker | SUPPORTED | 是（startup 校验）/ 显式（rebuild） | `VectorDBManager` + `document_loader.SCHEMA_VERSION` | Chroma collection metadata marker（`localagent_collection_contract_version=1`、`chunk_schema_version=kb_chunk_schema_v2`、`embedding_compatibility_digest`、`embedding_dimension`） | 空 collection 可初始化 marker；非空缺 marker / digest / dimension mismatch → REBUILD_REQUIRED；required KB 阻止 READY、optional KB 允许 READY_DEGRADED | startup 绝不自动 clear/rebuild；`bootstrap_local_kb.py --rebuild` 成功后 marker 最后发布 | 否 | `tests/test_chroma_persistence_contract.py` | digest 是 configured compatibility descriptor digest，不是 model artifact attestation |
| Automatic backup | NOT_IMPLEMENTED | 否 | 无 | 无 | 不提供自动/计划/云备份 | 无 | 否 | deployment contract docs guard | backup = manual stopped-server only（见 deployment runbook） |
| Automatic restore | NOT_IMPLEMENTED | 否 | 无 | 无 | 不提供 restore command | 无 | 否 | deployment contract docs guard | restore = manual stopped-server set replacement + explicit full preflight |
| Automatic deployment rollback | NOT_IMPLEMENTED | 否 | 无 | 无 | 不提供 automatic rollback/downgrade | 无 | 否 | deployment contract docs guard | code/artifact rollback 需匹配 pre-migration data restore |
| Downgrade migration | NOT_IMPLEMENTED | 否 | 无 | 无 | 不实现 reverse SQL / version graph / data downgrade | 无 | 否 | capability negative assertion | 回滚旧 binary 需恢复 pre-migration backup set |
| Online backup | NOT_IMPLEMENTED | 否 | 无 | 无 | 不在 live Server 复制 SQLite/Chroma 文件 | 无 | 否 | deployment contract docs guard | stopped-server backup 时 `.db` + `-wal` 视为同一 backup unit |
| Chroma internal schema migration | NOT_IMPLEMENTED | 否 | 无（NOT_LOCAL_SCHEMA_OWNER：LocalAgent 不拥有 Chroma internal SQLite schema） | 无 | 不读取/不修改 Chroma internal SQLite | 无 | 否 | `tests/test_chroma_persistence_contract.py` | LocalAgent 只拥有 collection/chunk/embedding compatibility contract；升级/rebuild 走 operator rebuild |
| Trace Contract v1 | SUPPORTED | 是 | `core/runtime/trace_contract.py` + `tracing.py`（六个稳定 operation） | recorder backend/safe snapshot | trace 失败隔离 | 无业务恢复 | 共享 model/router layer | trace contract/integration/hierarchy/propagation tests | PUBLIC_VERSIONED 语义合同；内部 Span 模型（`TraceContext`/`SpanHandle`/`SpanRecord`）不是公共 exporter payload |
| Consumer-neutral Trace export contract | SUPPORTED | 是（纯值/投影，无 transport） | `core/runtime/trace_export_contract.py` | 无（envelope 为不可变值；完整 `SpanRecord` 仍 memory-only） | 投影失败以固定 safe code 拒绝，不影响 Runtime/Tool/Output/Memory | 无 | 不适用 | `tests/test_trace_export_contract.py` | 无 exporter transport/queue/retry/batching；仅合同/值/投影 |
| Trace Contract Fingerprint | SUPPORTED | 是 | `TraceContractFingerprinter`（`core/runtime/trace_contract_fingerprint.py`，canonicalize+digest）；权威语义描述符由 `trace_export_contract.py`（Export Contract Semantic Owner）构建 | 无（code-owned 语义描述） | 确定性、易变排除由合同保证；value-domain/compatibility 语义变化必改指纹 | 无 | 不适用 | `tests/test_trace_contract_fingerprint.py` | 识别 schema+语义兼容性（含 type/presence/domain/compatibility 行为与 `CompatibilityReason` 词汇表）；不是 Trace 实例/Run/配置指纹 |
| TraceExporter protocol | SUPPORTED | 否（无默认 adapter；envelope-only Protocol） | `core/runtime/trace_exporter.py`（transport-neutral Protocol：`send(envelope)` + `close(timeout_seconds)`） | 无 | adapter 只接受 `TraceExportEnvelope`；raw `SpanRecord`/dict/mapping 禁止；transport 异常由 dispatcher worker 隔离 | 无 | 否 | `tests/test_trace_export_dispatcher.py` + `tests/test_trace_export_integration.py` | 无生产 adapter；protocol 无 retry/batch/serialization |
| TraceExportDispatcher | SUPPORTED | 否（optional 注入；生产 disabled-by-absence） | `core/runtime/trace_export_dispatcher.py`（APPLICATION_SCOPE：project_span invocation、compatibility consumption、bounded queue、单 daemon worker、drop/health、flush/close） | 无（queue/health 进程内；不持久化） | projection/compatibility/queue-full/closed/transport 失败 content-free drop；绝不改变 Runtime/Journal/Snapshot | 无 | 否 | dispatcher/integration/shutdown/metrics tests | 无 retry/batch/durable；queue/in-flight 进程崩溃可丢失；单 worker 吞吐受限 |
| Recorder completion observer integration | SUPPORTED | 否（`completion_observer=None` 默认） | `InMemorySpanRecorder` 单个可选 completion observer（构造注入；lock 外 best-effort 通知） | 无 | observer 失败隔离，本地 completed record 权威；close 收口 RECORDER_CLOSED final spans 仍通知 observer | 无 | 否 | `tests/test_trace_export_integration.py` | 单 observer；无 fan-out/registry/plugin framework |
| Trace export application lifecycle integration | SUPPORTED | 否（optional dispatcher target） | `ApplicationRuntimeServices` ordered targets（span_recorder < trace_export_dispatcher < snapshot_store）+ `RuntimeComponentResult`（FLUSH/CLOSE） | 无 | FLUSH/CLOSE component truth（`RUNTIME_TRACE_EXPORT_FLUSH/CLOSE_TIMEOUT/FAILED`）；失败不 abort 其余组件 | 无 | 否 | `tests/test_trace_export_shutdown.py` | 无 ShutdownReport schema 变化；GracefulShutdownCoordinator 仍 orchestration Owner |
| Trace export metrics | SUPPORTED | 否（`metrics_recorder=None` 默认） | dispatcher 事件/状态 best-effort 投影 + `core/runtime/metrics.py` 正式 descriptors | 进程内 recorder | metric 失败隔离；health counters 仍权威内部事实；高基数 label 禁止 | 无 | 否 | `tests/test_trace_export_metrics.py` | 仅诊断投影；drop reason 词表恰好 7 个（`projection_failed`/`incompatible`/`queue_full`/`closed`/`transport_failed`/`shutdown_timeout`/`worker_unavailable`）；failure stage 词表 6 个；无 ID/fingerprint/endpoint label；`worker_unavailable` 与 `stage=worker` 是互补事实 |
| Production external delivery | PARTIALLY_SUPPORTED | 否（默认 disabled；`LOCAL_AGENT_AGENTEVALOPS_TRACE_EXPORT_ENABLED=true` 显式启用） | `server.py::lifespan()` + `AgentEvalOpsTraceExporter`（PycURL/libcurl easy handle，单 POST/无 retry/无 redirect/无 proxy，硬总 deadline） | 无（发送不持久化；进程崩溃可丢失 queued/in-flight envelopes） | 非 2xx/3xx/deadline/response 超限/transport 失败 → bounded content-free `AgentEvalOpsTraceExportError`；remote 不可达不影响 readiness；无 startup probe | 无 | 否 | `tests/test_agent_evalops_trace_exporter.py`（真实本地 HTTP server probes：201/200/500/401/refused/deadline/trickle/>4096/3xx/reset/stale keepalive）+ dispatcher shutdown 测试 | WP4-C 已独立 Gate 且真实本地跨系统 E2E VERIFIED；无 retry/batch/queue durable/outbox/OTLP；API key 仅环境变量；response cap 4096 code-owned |
| Trace export retry | NOT_IMPLEMENTED | 否 | 无 | 无 | 每 accepted envelope 至多一次 transport attempt；失败不重入队 | 无 | 否 | dispatcher transport failure tests | 与 Model `RetryExecutor`（invocation 语义）无关 |
| Trace export batching | NOT_IMPLEMENTED | 否 | 无 | 无 | 每 queue item 一次 send；无 batch size/window/partial result | 无 | 否 | negative capability assertion | 未来 batch 需新 architecture decision |
| Durable trace export delivery | NOT_IMPLEMENTED | 否 | 无 | 无 outbox/spool/ack log | 进程崩溃可丢失 queued/in-flight envelopes；无 Trace/Journal/Snapshot replay | 无 | 否 | negative capability assertion | Known Limitation：ephemeral queue |
| Generic trace export wire serialization | NOT_IMPLEMENTED / DEFERRED | 否 | 无 | 无 | 保持 typed-envelope protocol；无 `to_json`/stable wire JSON/HTTP payload | 无 | 否 | negative capability assertion | 已实现的是 AgentEvalOps-specific `TraceExportEnvelope` v1 wire（`serialize_trace_export_envelope`，见下），不是 generic consumer-neutral wire；generic consumer-neutral / OTLP 等通用 wire 仍 NOT_IMPLEMENTED / DEFERRED，需独立 versioned decision |
| AgentEvalOps adapter | PARTIALLY_SUPPORTED | 否（enabled 时经 `server.py::lifespan()` 构造并注入 `TraceExportDispatcher`；disabled 保持 WP4-B 现状） | `core/runtime/agent_evalops_trace_exporter.py`（envelope-only；复用 WP4-B serializer bytes 为 body；`POST <base_url>/integrations/localagent/v1/trace-envelopes`；CONNECTTIMEOUT_MS=500 / TIMEOUT_MS=3000 / NOSIGNAL=1；AsynchDNS 启动门禁；HTTPS 目标要求 TLS；TLS verify ON；`Expect:` 禁用） | 无 | 启动时 PycURL/AsynchDNS/HTTPS 能力缺失、非法 URL、deadline 不满足 close invariant 均 startup-fatal；send 失败为 bounded content-free code（`AGENTEVALOPS_TRACE_EXPORT_DEADLINE_EXCEEDED` / `_FAILED` / `_RESPONSE_TOO_LARGE`） | 无 | 否 | `tests/test_agent_evalops_trace_exporter.py` + `tests/test_trace_export_serialization.py` + startup configuration 新增测试 | WP4-C 已独立 Gate 且真实本地跨系统 E2E VERIFIED（首写 201 / 精确 replay 200 / 冲突 409）；无 retry/batch/queue durable/outbox；response body cap 4096；不做 redirect/proxy；不记录 API key/body/envelope/URL |
| OpenTelemetry / OTLP | NOT_IMPLEMENTED | 否 | 无 | 无 | 不导入/使用 OTel SDK；OTel-shaped helper ≠ OTel implementation | 无 | 否 | negative capability assertion | 无 TracerProvider/Collector/OTLP/sampling/propagation |
| Trace Instance Fingerprint | NOT_IMPLEMENTED | 否 | 无 | 无 | 不生成 per-Run/per-Span trace hash | 无 | 否 | negative capability assertion | 延后/out of scope |
| Run Configuration Fingerprint | NOT_IMPLEMENTED | 否 | 无 | 无 | `runtime_version` 等 `not_configured` 占位不构成统一配置指纹 | 无 | 否 | negative capability assertion | 缺统一真实 Owner；不得伪造配置兼容事实 |

## Status summary

- SUPPORTED：Coordinated、显式 Legacy、Parallel engine、Budget、Retry、Model fallback、Circuit breaker、Tool contract/evidence/lease、Tool Registry、Tool Descriptor、Static startup registration、Duplicate Tool identity fail-closed、Internal Tool discovery、All-Tool adapter execution、Retrieval、Streaming、Journal-first、Observability、Trace、Trace Contract v1（PUBLIC_VERSIONED）、Consumer-neutral Trace export contract（PUBLIC_VERSIONED）、Trace Contract Fingerprint（PUBLIC_VERSIONED）、TraceExporter protocol、TraceExportDispatcher、Recorder completion observer integration、Trace export application lifecycle integration、Trace export metrics、Disconnect、Worker tracking、Graceful shutdown、确定性 Fault Injection 测试能力、Health / Readiness endpoint、Startup readiness handshake、HTTP raw-body payload boundary、HTTP semantic field boundary、Persistence preflight、Explicit SQLite migration、Memory versioned migration、Journal known physical migration、Checkpoint explicit recreate、Chroma compatibility marker。
- SUPPORTED：SQL Injection protection（仅限 current LocalAgent production SQLite inventory；code-owned statement structure、DB-API binding与test-only AST Gate）。
- PARTIALLY_SUPPORTED：Prompt Injection protection（确定性role/trust binding与typed denial integrity；不保证模型免受恶意自然语言影响）、Snapshot、Recovery validation（后两者均为 opt-in/validation-only）、Production external delivery（WP4-C 最小 adapter，已独立 Gate）、AgentEvalOps adapter（WP4-C 最小 adapter，已独立 Gate；真实本地跨系统 E2E VERIFIED）。
- CONTRACT_ONLY：能力级无；个别 FaultPoint 可在 `FaultPointSupportReport` 中标为 CONTRACT_ONLY。
- NOT_IMPLEMENTED：WAF / generic abuse protection、generic Prompt Injection classifier、Recovery execution、Replay、Production Fault Enablement、Random / Probabilistic Chaos、Cross-process Registry、Exactly-once、Automatic compensation、Step result rehydration、Multi-process server、Windows Service wrapper、Docker / Compose、Continuous Health/Readiness monitoring、Post-start dependency aggregate health、Version compatibility（DEFER_TO_WP4）、Automatic backup、Automatic restore、Automatic deployment rollback、Downgrade migration、Online backup、Chroma internal schema migration（NOT_LOCAL_SCHEMA_OWNER）、Trace export retry、Trace export batching、Durable trace export delivery、Generic trace export wire serialization（DEFERRED）、OpenTelemetry / OTLP、Trace Instance Fingerprint、Run Configuration Fingerprint。
- LEGACY_ONLY：当前矩阵没有仅 Legacy 拥有且 Coordinated 缺失的目标能力。
- DEPRECATED：能力级无；兼容字段见架构文档的 Deprecated / Compatibility Matrix。
